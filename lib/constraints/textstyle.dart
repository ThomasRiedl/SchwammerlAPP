import 'package:flutter/material.dart';

/// A variable that holds the text style for a bold and large font.
var txt = const TextStyle(
  fontSize: 22,
  fontWeight: FontWeight.bold,
);

/// A variable that holds the text style for a medium font.
var txt2 = const TextStyle(
  fontSize: 18,
);
